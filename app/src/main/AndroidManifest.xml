<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fondn.leadsversatile">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".Start">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".logINandlogOut.SignInClass" />
        <activity android:name=".logINandlogOut.SignUpClass" />
        <activity android:name=".Others" />
        <activity android:name=".logINandlogOut.LoggedIn" />
        <activity android:name=".others.OthersTools" />
        <activity android:name=".dialpaddirectory.CallingActivity"></activity>
    </application>

</manifest>